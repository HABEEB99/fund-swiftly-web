import { LoginForm } from "@/components/forms/login-form";

const LoginPage = () => {
  return (
    <div className="w-full h-full wrapper flex items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
